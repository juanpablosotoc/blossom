version: "3.9"

services:
  ai:
    build:
      context: ../../            # repo root (so we can COPY libraries/ and services/)
      dockerfile: services/ai/Dockerfile
    env_file:
      - env/dev.env
    ports:
      - "80:80"
    environment:
      - PYTHONUNBUFFERED=1
    working_dir: /app